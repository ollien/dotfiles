---
- name: Bootstrap
  hosts: localhost
  gather_facts: true
  roles:
    - role: packages
      vars:
        packages_to_install:
          - neovim
          - zsh
          - curl
    - role: chezmoi
